# Database

SQLite database and schema for RetroPlay.

## Files

- `retroplay.db` - SQLite database file containing user data, playlists, and music library
- `schema.sql` - Database schema definition
